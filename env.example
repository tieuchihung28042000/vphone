# VPhone Production Configuration
# Copy this file to .env and modify as needed

# Environment
NODE_ENV=production

# Domain Configuration (IMPORTANT: Change this for your domain)
DOMAIN=nguyenkieuanh.com
# For local development, use: DOMAIN=localhost

# SSL Configuration (for Let's Encrypt)
SSL_EMAIL=admin@nguyenkieuanh.com

# MongoDB Configuration 
MONGO_ROOT_USERNAME=admin
MONGO_ROOT_PASSWORD=VPhone2025!SecurePass
MONGO_DB_NAME=vphone_production

# JWT Secret (IMPORTANT: Change this in production!)
JWT_SECRET=VPhone2025!ProductionJWT#NguyenKieuAnh$SecureKey

# Email Configuration (Gmail SMTP)
EMAIL_USER=vphone24h3@gmail.com
EMAIL_PASS=ftxhkismjvdqzawp

# Port Configuration (Default Docker ports)
# Chỉ thay đổi nếu có conflict trên VPS
NGINX_PORT=80
NGINX_SSL_PORT=443
FRONTEND_PORT=3000
BACKEND_PORT=4000
MONGODB_PORT=27017

# Resource Limits (for VPS with limited resources)
# Uncomment and adjust if needed
# MAX_MEMORY=1g
# MAX_CPUS=1.0 